<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.centit.shopping.dao.ShoppingPayLimitDao">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.centit.shopping.po.ShoppingPayLimit">
        <result column="point_pay" property="pointPay" />
        <result column="balance_pay" property="balancePay" />
        <result column="balance_recharge" property="balanceRecharge" />
    </resultMap>

    <!-- 通用查询结果列(横着排列) -->
    <sql id="Base_Column_List1">
        point_pay, balance_pay, balance_recharge
    </sql>

    <!-- 通用查询结果列(竖着排列) -->
    <sql id="Base_Column_List2">
        point_pay
          ,
        balance_pay
          ,
        balance_recharge
    </sql>

    <!-- 通用查询条件 -->
    <sql id="Base_Where_List">
        <if test="pointPay != null and pointPay != ''">
            <![CDATA[
                AND point_pay = #{pointPay ,jdbcType=VARCHAR}
            ]]>
        </if>
        <if test="balancePay != null and balancePay != ''">
            <![CDATA[
                AND balance_pay = #{balancePay ,jdbcType=VARCHAR}
            ]]>
        </if>
        <if test="balanceRecharge != null and balanceRecharge != ''">
            <![CDATA[
                AND balance_recharge = #{balanceRecharge ,jdbcType=VARCHAR}
            ]]>
        </if>
    </sql>


    <!--  新增 -->
    <insert id="insert" parameterType="com.centit.shopping.po.ShoppingPayLimit">
        <![CDATA[
			insert into shopping_pay_limit
		]]>
        <trim prefix="(" suffix=")" suffixOverrides="," >
            <if test="pointPay != null and pointPay != ''" >
                <![CDATA[
                    point_pay,
                ]]>
            </if>
            <if test="balancePay != null and balancePay != ''" >
                <![CDATA[
                    balance_pay,
                ]]>
            </if>
            <if test="balanceRecharge != null and balanceRecharge != ''" >
                <![CDATA[
                    balance_recharge,
                ]]>
            </if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides="," >
            <if test="pointPay != null and pointPay != ''" >
                <![CDATA[
                    #{pointPay,jdbcType=VARCHAR},
                ]]>
            </if>
            <if test="balancePay != null and balancePay != ''" >
                <![CDATA[
                    #{balancePay,jdbcType=VARCHAR},
                ]]>
            </if>
            <if test="balanceRecharge != null and balanceRecharge != ''" >
                <![CDATA[
                    #{balanceRecharge,jdbcType=VARCHAR},
                ]]>
            </if>
        </trim>
    </insert>

    <!-- 更新 -->
    <update id="update" parameterType="com.centit.shopping.po.ShoppingPayLimit" >
        <![CDATA[
        	update shopping_pay_limit
        ]]>
        <set >
            <if test="pointPay != null" >
                <![CDATA[
                    point_pay = #{pointPay,jdbcType=VARCHAR},
                ]]>
            </if>
            <if test="balancePay != null" >
                <![CDATA[
                    balance_pay = #{balancePay,jdbcType=VARCHAR},
                ]]>
            </if>
            <if test="balanceRecharge != null" >
                <![CDATA[
                    balance_recharge = #{balanceRecharge,jdbcType=VARCHAR},
                ]]>
            </if>
        </set>
        <where>
        </where>
    </update>


    <!-- 删除 -->
    <delete id="delete"  parameterType="com.centit.shopping.po.ShoppingPayLimit">
        <![CDATA[
            delete from shopping_pay_limit
        ]]>
        <where>
        </where>
    </delete>


    <!--  查询详情 -->
    <select id="queryDetail" resultMap="BaseResultMap" parameterType="com.centit.shopping.po.ShoppingPayLimit">
        <![CDATA[
            select
        ]]>
        <include refid="Base_Column_List1" />
        <![CDATA[
            from shopping_pay_limit t
        ]]>
        <where>
        </where>
    </select>

    <!--  查询列表 -->
    <select id="queryList" resultMap="BaseResultMap" parameterType="java.util.HashMap">
        <![CDATA[
            select
        ]]>
        <include refid="Base_Column_List1" />
        <![CDATA[
            from shopping_pay_limit t
        ]]>
        <where>
            <include refid="Base_Where_List" />
        </where>
    </select>

</mapper>
