<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.centit.shopping.dao.ShoppingRechargeDiscountDao">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.centit.shopping.po.ShoppingRechargeDiscount">
        <result column="discount_num" property="discountNum" />
    </resultMap>

    <!-- 通用查询结果列(横着排列) -->
    <sql id="Base_Column_List1">
        discount_num
    </sql>

    <!-- 通用查询结果列(竖着排列) -->
    <sql id="Base_Column_List2">
        discount_num
    </sql>

    <!-- 通用查询条件 -->
    <sql id="Base_Where_List">
        <if test="discountNum != null and discountNum != ''">
            <![CDATA[
                AND discount_num = #{discountNum ,jdbcType=VARCHAR}
            ]]>
        </if>
    </sql>


    <!--  新增 -->
    <insert id="insert" parameterType="com.centit.shopping.po.ShoppingRechargeDiscount">
        <![CDATA[
			insert into shopping_recharge_discount
		]]>
        <trim prefix="(" suffix=")" suffixOverrides="," >
            <if test="discountNum != null and discountNum != ''" >
                <![CDATA[
                    discount_num,
                ]]>
            </if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides="," >
            <if test="discountNum != null and discountNum != ''" >
                <![CDATA[
                    #{discountNum,jdbcType=VARCHAR},
                ]]>
            </if>
        </trim>
    </insert>

    <!-- 更新 -->
    <update id="update" parameterType="com.centit.shopping.po.ShoppingRechargeDiscount" >
        <![CDATA[
        	update shopping_recharge_discount
        ]]>
        <set >
            <if test="discountNum != null and discountNum != ''" >
                <![CDATA[
                    discount_num = #{discountNum,jdbcType=VARCHAR},
                ]]>
            </if>
        </set>
        <where>
        </where>
    </update>


    <!-- 删除 -->
    <delete id="delete"  parameterType="com.centit.shopping.po.ShoppingRechargeDiscount">
        <![CDATA[
            delete from shopping_recharge_discount
        ]]>
        <where>
        </where>
    </delete>


    <!--  查询详情 -->
    <select id="queryDetail" resultMap="BaseResultMap" parameterType="com.centit.shopping.po.ShoppingRechargeDiscount">
        <![CDATA[
            select
        ]]>
        <include refid="Base_Column_List1" />
        <![CDATA[
            from shopping_recharge_discount t
        ]]>
        <where>
        </where>
    </select>

    <!--  查询列表 -->
    <select id="queryList" resultMap="BaseResultMap" parameterType="java.util.HashMap">
        <![CDATA[
            select
        ]]>
        <include refid="Base_Column_List1" />
        <![CDATA[
            from shopping_recharge_discount t
        ]]>
        <where>
            <include refid="Base_Where_List" />
        </where>
    </select>

</mapper>
